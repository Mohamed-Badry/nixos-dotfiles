default:
    @just --list

# Sync project dependencies
sync:
    uv sync

# Run the project
run *ARGS:
    uv run python {{ARGS}}

# Lint and format
check:
    ruff check .
    ty check

# Auto-fix lint issues
fix:
    ruff check --fix .
    ruff format .
